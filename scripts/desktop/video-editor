#!/bin/bash
export DISPLAY="${DISPLAY:-:1}"
export MESA_GLTHREAD=true
export vblank_mode=0
notify-send "🎬 Premiere & DaVinci Studio" "Launching Shotcut 4K Multi-Track Video Editor..." -i /usr/share/icons/virgox/videoeditor.svg 2>/dev/null || true
exec /usr/local/bin/shotcut "$@"
